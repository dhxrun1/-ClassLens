services:
  - type: web
    name: lens-attendance-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.8"
      - key: DATABASE_URL
        fromDatabase:
          name: lens-attendance-db
          property: connectionString

databases:
  - name: lens-attendance-db
    plan: free
    databaseName: lens_attendance
    ipAllowList: []
